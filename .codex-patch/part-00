diff --git a/app/globals.css b/app/globals.css
index 1a6adb5..abb96c0 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -913,7 +913,11 @@ select:focus-visible {
   width: 100%;
   margin: 0 0 0.34em;
   padding: 0;
+  border: 0;
+  appearance: none;
+  background: transparent;
   color: var(--cream);
+  font-family: inherit;
   font-size: clamp(36px, 3.85vw, 60px);
   font-weight: 710;
   line-height: 1.12;
@@ -924,17 +928,6 @@ select:focus-visible {
   transition: opacity 130ms ease-out;
 }
 
-.line-seek-target {
-  position: absolute;
-  z-index: 0;
-  inset: 0;
-  width: 100%;
-  padding: 0;
-  border: 0;
-  background: transparent;
-  cursor: pointer;
-}
-
 .lyric-line:hover {
   opacity: 0.74;
 }
@@ -1020,9 +1013,9 @@ select:focus-visible {
   filter: drop-shadow(0 0 9px rgba(255, 250, 245, 0.16));
 }
 
-.lyric-word:focus-visible {
+.lyric-line:focus-visible {
   outline: 2px solid rgba(255, 116, 88, 0.88);
-  outline-offset: 4px;
+  outline-offset: 6px;
 }
 
 .lyric-line.is-adlib .lyric-word {
diff --git a/app/page.tsx b/app/page.tsx
index 9c1e852..c5124f2 100644
--- a/app/page.tsx
+++ b/app/page.tsx
@@ -4,6 +4,7 @@ import {
   type CSSProperties,
   type ChangeEvent,
   type DragEvent,
+  memo,
   useCallback,
   useEffect,
   useMemo,
@@ -32,6 +33,13 @@ import {
   Waves,
   X,
 } from "lucide-react";
+import {
+  buildLineWordOffsets,
+  buildPrefixMaxEnds,
+  findActiveIntervalIndexes,
+  findLastStartedIndex,
+  pollingRetryDelay,
+} from "./lyric-timeline.js";
 import { wordFillAt } from "./lyric-timing.js";
 type ProcessingStage =
   | "idle"
@@ -92,6 +100,7 @@ type BackendHealth = {
   cuda: boolean;
   device: string;
   ffmpeg: boolean;
+  ffprobe?: boolean;
   demucs: boolean;
   transformers: boolean;
 };
@@ -99,7 +108,7 @@ type BackendHealth = {
 const ACCEPTED_EXTENSIONS = ["mp3", "wav", "flac", "m4a", "aac", "ogg", "webm"];
 const MAX_FILE_SIZE = 500 * 1024 * 1024;
 const LYRIC_LOOKAHEAD_SECONDS = 0.055;
-const PLAYER_CLOCK_INTERVAL_MS = 80;
+const PLAYER_CLOCK_INTERVAL_MS = 100;
 const LOCAL_API_URL =
   process.env.NEXT_PUBLIC_LYRICWAVE_API_URL?.replace(/\/$/, "") ?? "http://127.0.0.1:8008";
 
@@ -140,7 +149,7 @@ function parseTrackName(fileName: string) {
 
 function isAudioFile(file: File) {
   const extension = file.name.split(".").pop()?.toLowerCase() ?? "";
-  return file.type.startsWith("audio/") || ACCEPTED_EXTENSIONS.includes(extension);
+  return ACCEPTED_EXTENSIONS.includes(extension);
 }
 
 function groupIntoLines(words: TimedWord[]): LyricLine[] {
@@ -212,6 +221,18 @@ function apiMessage(payload: unknown, fallback: string) {
   return fallback;
 }
 
+class RetryablePollingError extends Error {}
+
+function isRetryablePollingError(error: unknown) {
+  return (
+    error instanceof RetryablePollingError ||
+    error instanceof SyntaxError ||
+    error instanceof TypeError ||
+    (error instanceof DOMException &&
+      ["AbortError", "NetworkError", "TimeoutError"].includes(error.name))
+  );
+}
+
 functio