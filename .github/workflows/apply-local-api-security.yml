name: Apply localhost API security integration

on:
  push:
    branches: [codex/secure-local-api]
    paths:
      - scripts/apply-local-api-security.py
      - .github/workflows/apply-local-api-security.yml

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, 'Integrate localhost API request security') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/secure-local-api
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/apply-local-api-security.py
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
      - run: npm run test:frontend
      - run: python -m pip install -r backend/requirements-test.txt
      - run: python -m compileall -q backend
      - run: python -m unittest backend -v
      - name: Remove temporary patch machinery
        run: rm scripts/apply-local-api-security.py .github/workflows/apply-local-api-security.yml
      - name: Commit integrated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/page.tsx backend/server.py backend/test_local_security.py tests/rendered-html.test.mjs README.md scripts/apply-local-api-security.py .github/workflows/apply-local-api-security.yml
          git commit -m "Integrate localhost API request security"
          git push origin HEAD:codex/secure-local-api
